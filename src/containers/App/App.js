import React, { Fragment } from 'react';

import 'assets/utils/normalize.css';
import 'assets/globalStyles.scss';

const App = ({ children }) => (
    <Fragment>
        {children}
    </Fragment>
);

export default App;
